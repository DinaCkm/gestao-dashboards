import type { Express, Request, Response } from "express";
import { sdk } from "./_core/sdk";
import { PROGRAMA_INTEGRACAO_CATALOG, UNIDADES_INTEGRACAO } from "./programaIntegracaoCatalog";

function safeJson(value: unknown) {
  return JSON.stringify(value).replace(/</g, "\\u003c").replace(/>/g, "\\u003e").replace(/&/g, "\\u0026");
}

const baseCss = `
:root{font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:#172033;background:#f5f7fa;line-height:1.45}*{box-sizing:border-box}body{margin:0;background:#f5f7fa}button,input,select,textarea{font:inherit}.top{background:#fff;border-bottom:1px solid #e3e7ee;padding:15px 22px;display:flex;align-items:center;gap:12px;position:sticky;top:0;z-index:20}.brand{font-weight:800;color:#17543e}.mark{width:38px;height:38px;border-radius:10px;background:#17543e;color:#fff;display:grid;place-items:center;font-weight:800}.wrap{max-width:1050px;margin:28px auto;padding:0 18px 50px}.card{background:#fff;border:1px solid #e1e6ed;border-radius:14px;box-shadow:0 3px 14px rgba(28,42,61,.05);padding:22px;margin-bottom:16px}h1{font-size:26px;margin:0 0 7px}h2{font-size:18px;margin:0 0 14px}h3{font-size:15px;margin:0 0 10px}.muted{color:#677184}.small{font-size:12px}.intro{max-width:86ch;color:#4d5869}.intro p{margin:0 0 10px}.field{margin-bottom:16px}.field label{display:block;font-weight:650;font-size:13px;margin:0 0 6px}.field input,.field select,.field textarea{width:100%;border:1px solid #cfd6df;border-radius:9px;padding:10px 11px;background:#fff}.field textarea{min-height:96px;resize:vertical}.req{color:#a72e2e}.hint{font-size:12px;color:#697487;margin-top:5px}.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.section{border-top:1px solid #e6eaf0;padding-top:18px;margin-top:18px}.section-title{text-transform:uppercase;letter-spacing:.06em;font-size:12px;font-weight:800;color:#17543e;margin-bottom:13px}.btn{border:1px solid #cbd3dc;background:#fff;border-radius:9px;padding:9px 14px;cursor:pointer}.btn.primary{background:#17543e;border-color:#17543e;color:#fff}.btn.danger{color:#9a2a2a}.btn:disabled{opacity:.5;cursor:not-allowed}.actions{display:flex;justify-content:space-between;gap:10px;margin-top:22px}.steps{display:flex;gap:5px;margin:14px 0}.step{height:5px;flex:1;border-radius:99px;background:#e1e6eb}.step.on,.step.done{background:#2d8568}.scale{display:flex;gap:6px;flex-wrap:wrap}.scale label{font-weight:500;border:1px solid #d6dce4;border-radius:8px;padding:7px 9px;cursor:pointer}.multi{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}.multi label{font-size:12px;border:1px solid #dde2e8;border-radius:8px;padding:7px}.err{border-color:#c74343!important;background:#fff7f7!important}.msg{padding:11px 13px;border-radius:9px;margin:10px 0}.msg.error{background:#fff0f0;color:#8c2929;border:1px solid #f0caca}.msg.ok{background:#edf8f3;color:#17543e;border:1px solid #cce7da}.proto{font-size:18px;font-weight:800;letter-spacing:.04em}.tabs{display:flex;gap:6px;flex-wrap:wrap;margin-bottom:16px}.tab{border:1px solid #d7dde5;background:#fff;padding:8px 12px;border-radius:9px;cursor:pointer}.tab.on{background:#17543e;color:#fff;border-color:#17543e}.kpis{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}.kpi{background:#fff;border:1px solid #e0e5eb;border-radius:12px;padding:15px}.kpi b{font-size:26px;display:block}.tablewrap{overflow:auto}table{width:100%;border-collapse:collapse;font-size:13px}th,td{text-align:left;padding:9px 10px;border-bottom:1px solid #e7ebf0;vertical-align:top}th{font-size:11px;text-transform:uppercase;color:#687487}.badge{display:inline-block;border-radius:99px;background:#edf2f5;padding:3px 8px;font-size:11px}.linkbox{display:flex;gap:8px;align-items:center}.linkbox code{flex:1;padding:8px;background:#f4f6f8;border-radius:7px;overflow:auto}.hidden{display:none}.modal{position:fixed;inset:0;background:rgba(15,23,42,.45);display:grid;place-items:center;padding:20px;z-index:50}.modal>.card{width:min(700px,100%);max-height:90vh;overflow:auto;margin:0}@media(max-width:720px){.grid,.kpis{grid-template-columns:1fr}.multi{grid-template-columns:repeat(2,minmax(0,1fr))}.wrap{margin-top:16px}.top{padding:12px}.actions{position:sticky;bottom:0;background:#fff;padding-top:10px}}
`;

function publicFormHtml(slug: string) {
  const form = PROGRAMA_INTEGRACAO_CATALOG[slug];
  if (!form) return null;
  const data = safeJson({ form, unidades: UNIDADES_INTEGRACAO });
  return `<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${form.name} · EcoLíder</title><style>${baseCss}</style></head><body>
  <div class="top"><div class="mark">CKM</div><div><b>Programa de Integração · Sebrae/TO</b><div class="small muted">Formulário oficial hospedado no EcoLíder</div></div></div>
  <main class="wrap" id="app"></main>
  <script>const DATA=${data};
  const f=DATA.form;let page=0,sent=false,result=null,error='';const values={nomeColaborador:'',unidade:'',dataInicio:'',emailColaborador:'',respondentName:'',cycleValue:'',role:'',answers:{}};
  const pages=[{title:'Identificação',identity:true},...f.sections];
  function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
  function optionValue(o){return typeof o==='string'?o:o.value}function optionLabel(o){return typeof o==='string'?o:o.label}
  function field(q){const v=values.answers[q.code]||'';let ctl='';if(q.type==='scale'){ctl='<div class="scale">'+[0,1,2,3,4,5].map(n=>'<label><input type="radio" name="'+esc(q.code)+'" data-code="'+esc(q.code)+'" value="'+n+'" '+(String(v)===String(n)?'checked':'')+'> '+n+(n===0?' · sem opinião':'')+'</label>').join('')+'</div>';}else if(q.type==='select'){ctl='<select data-code="'+esc(q.code)+'"><option value="">— escolha —</option>'+q.options.map(o=>'<option value="'+esc(optionValue(o))+'" '+(v===optionValue(o)?'selected':'')+'>'+esc(optionLabel(o))+'</option>').join('')+'</select>';}else if(q.type==='multi'){const set=new Set(String(v).split(',').map(x=>x.trim()).filter(Boolean));ctl='<div class="multi">'+q.options.map(o=>{const x=optionValue(o);return '<label><input type="checkbox" data-multi="'+esc(q.code)+'" value="'+esc(x)+'" '+(set.has(x)?'checked':'')+'> '+esc(optionLabel(o))+'</label>';}).join('')+'</div>';}else if(q.type==='textarea'){ctl='<textarea data-code="'+esc(q.code)+'">'+esc(v)+'</textarea>';}else{const type=q.type==='date'?'date':'text',inputmode=(q.type==='cpf'||q.type==='tel')?' inputmode="numeric"':'';ctl='<input type="'+type+'"'+inputmode+' data-code="'+esc(q.code)+'" value="'+esc(v)+'">';}return '<div class="field"><label>'+esc(q.label)+(q.required!==false?' <span class="req">*</span>':'')+'</label>'+ctl+(q.hint?'<div class="hint">'+esc(q.hint)+'</div>':'')+'</div>';}
  function identity(){let h='<div class="intro">'+f.intro.map(p=>'<p>'+esc(p)+'</p>').join('')+'</div><div class="section"><div class="section-title">Sobre quem esta resposta é</div><div class="field"><label>Nome completo do colaborador <span class="req">*</span></label><input data-meta="nomeColaborador" value="'+esc(values.nomeColaborador)+'"><div class="hint">Escreva o nome como está no cadastro.</div></div>';
   if(f.identity.unidade)h+='<div class="field"><label>Unidade <span class="req">*</span></label><select data-meta="unidade"><option value="">— selecione —</option>'+DATA.unidades.map(u=>'<option '+(values.unidade===u?'selected':'')+'>'+esc(u)+'</option>').join('')+'</select></div>';
   if(f.identity.dataInicio)h+='<div class="field"><label>Data de início do colaborador <span class="req">*</span></label><input type="date" data-meta="dataInicio" value="'+esc(values.dataInicio)+'"></div>';
   if(f.identity.email)h+='<div class="field"><label>E-mail do colaborador</label><input type="email" data-meta="emailColaborador" value="'+esc(values.emailColaborador)+'"></div>';
   if(f.identity.cycle)h+='<div class="field"><label>Período <span class="req">*</span></label><select data-meta="cycleValue"><option value="">— escolha —</option>'+f.cycleOptions.map(o=>'<option value="'+esc(o.value)+'" '+(values.cycleValue===o.value?'selected':'')+'>'+esc(o.label)+'</option>').join('')+'</select></div>';
   if(f.identity.role)h+='<div class="field"><label>Você está respondendo como <span class="req">*</span></label><select data-meta="role"><option value="">— escolha —</option><option '+(values.role==='Gestor'?'selected':'')+'>Gestor</option><option '+(values.role==='Anjo'?'selected':'')+'>Anjo</option></select></div>';
   if(f.identity.respondent)h+='<div class="field"><label>Seu nome completo <span class="req">*</span></label><input data-meta="respondentName" value="'+esc(values.respondentName)+'"></div>';
   return h+'</div>';}
  function validate(){error='';if(page===0){if(!values.nomeColaborador.trim())error='Informe o nome completo do colaborador.';else if(f.identity.unidade&&!values.unidade)error='Escolha a unidade.';else if(f.identity.dataInicio&&!values.dataInicio)error='Informe a data de início.';else if(f.identity.cycle&&!values.cycleValue)error='Escolha o período.';else if(f.identity.role&&!values.role)error='Informe se a resposta é do Gestor ou do Anjo.';else if(f.identity.respondent&&!values.respondentName.trim())error='Informe seu nome completo.';}else{for(const q of pages[page].questions){const v=values.answers[q.code];if(q.required!==false&&(!v&&String(v)!=='0')){error='Preencha: '+q.label;break;}if(q.type==='textarea'&&v&&String(v).trim().length<10){error='A resposta de texto precisa ter pelo menos 10 caracteres.';break;}}}return !error;}
  function sync(){document.querySelectorAll('[data-meta]').forEach(el=>values[el.dataset.meta]=el.value);document.querySelectorAll('[data-code]').forEach(el=>{if(el.type==='radio'){if(el.checked)values.answers[el.dataset.code]=el.value;}else values.answers[el.dataset.code]=el.value;});document.querySelectorAll('[data-multi]').forEach(el=>{const code=el.dataset.multi;const all=[...document.querySelectorAll('[data-multi="'+code+'"]')].filter(x=>x.checked).map(x=>x.value);values.answers[code]=all.join(', ');});}
  async function submit(){sync();if(!validate()){render();return;}const cyc=f.cycleOptions?.find(o=>o.value===values.cycleValue);const payload={nomeColaborador:values.nomeColaborador,unidade:values.unidade,dataInicio:values.dataInicio,emailColaborador:values.emailColaborador,respondentName:f.identity.respondent?values.respondentName:values.nomeColaborador,cycle:cyc?cyc.cycle:0,cycleLabel:cyc?cyc.label:'',role:values.role,answers:values.answers};try{const r=await fetch('/api/public/programa-integracao/forms/${slug}/responses',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const j=await r.json();if(!r.ok||!j.ok)throw new Error(j.erro||'Não foi possível registrar a resposta.');result=j;sent=true;render();scrollTo(0,0);}catch(e){error=e.message;render();}}
  function render(){const app=document.getElementById('app');if(sent){app.innerHTML='<div class="card"><div class="msg ok"><h1>Resposta enviada com sucesso</h1><p>Obrigado pela participação.</p><div>Protocolo: <span class="proto">'+esc(result.protocolo||'')+'</span></div>'+(result.pendente?'<p class="small">A resposta foi recebida e está aguardando conferência administrativa para vinculação ao processo correto. Ela não será perdida.</p>':'')+'</div>'+(f.outro?'<div class="intro">'+f.outro.map(p=>'<p>'+esc(p)+'</p>').join('')+'</div>':'')+'</div>';return;}const pg=pages[page];let body=pg.identity?identity():'<div class="section-title">'+esc(pg.title||'Formulário')+'</div>'+pg.questions.map(field).join('');app.innerHTML='<div class="card"><h1>'+esc(f.name)+'</h1><p class="muted">'+esc(f.description)+'</p><div class="steps">'+pages.map((_,i)=>'<span class="step '+(i<page?'done':i===page?'on':'')+'"></span>').join('')+'</div><div class="small muted" style="text-align:center;margin-bottom:18px">Página '+(page+1)+' de '+pages.length+'</div>'+(error?'<div class="msg error">'+esc(error)+'</div>':'')+body+'<div class="actions">'+(page?'<button class="btn" id="back">← Voltar</button>':'<span></span>')+(page<pages.length-1?'<button class="btn primary" id="next">Avançar →</button>':'<button class="btn primary" id="send">Enviar resposta</button>')+'</div></div>';document.getElementById('back')?.addEventListener('click',()=>{sync();page--;error='';render();scrollTo(0,0)});document.getElementById('next')?.addEventListener('click',()=>{sync();if(validate()){page++;error='';render();scrollTo(0,0)}else render()});document.getElementById('send')?.addEventListener('click',submit);}
  render();</script></body></html>`;
}

function adminHtml() {
  const links = Object.values(PROGRAMA_INTEGRACAO_CATALOG).map(f => ({ name:f.name, slug:f.slug, path:`/formularios/${f.slug}` }));
  return `<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Processos Integração · EcoLíder</title><style>${baseCss}.adminnav{display:flex;gap:8px;margin-left:auto}.process{cursor:pointer}.process:hover{background:#f7faf9}</style></head><body>
  <div class="top"><div class="mark">CKM</div><div><b>Processos Integração</b><div class="small muted">Programa de Integração · EcoLíder</div></div><div class="adminnav"><a class="btn" href="/">Voltar ao EcoLíder</a></div></div><main class="wrap" id="app"><div class="card">Carregando...</div></main>
  <script>const LINKS=${safeJson(links)};let state=null,tab='painel',editId=null,error='';const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  async function api(url,opt){const r=await fetch(url,opt);const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||j.erro||'Falha na plataforma');return j}
  async function load(){try{const j=await api('/api/programa-integracao/bootstrap');state=j.state;render()}catch(e){document.getElementById('app').innerHTML='<div class="msg error">'+esc(e.message)+'</div>'}}
  function ordem(){return state?.config?.ordem||[]}function processos(){return ordem().map(id=>[id,state.processos[id]]).filter(x=>x[1])}function responses(){return processos().flatMap(([id,p])=>(p.resp||[]).map(r=>({id,p,r}))) }function pend(){return state?.config?.respostasPendentes||[]}
  function nav(){return '<div class="tabs">'+[['painel','Painel'],['pessoas','Pessoas'],['formularios','Formulários'],['respostas','Respostas'],['pendentes','Pendentes de vinculação'],['config','Configurações']].map(x=>'<button class="tab '+(tab===x[0]?'on':'')+'" data-tab="'+x[0]+'">'+x[1]+(x[0]==='pendentes'&&pend().length?' ('+pend().length+')':'')+'</button>').join('')+'</div>'}
  function painel(){const ps=processos(),rs=responses();return '<div class="kpis"><div class="kpi"><span class="muted small">Processos</span><b>'+ps.length+'</b></div><div class="kpi"><span class="muted small">Ativos</span><b>'+ps.filter(x=>x[1].situacao!=='encerrado').length+'</b></div><div class="kpi"><span class="muted small">Respostas</span><b>'+rs.length+'</b></div><div class="kpi"><span class="muted small">Pendentes</span><b>'+pend().length+'</b></div></div><div class="card" style="margin-top:16px"><h2>Processos recentes</h2>'+processTable(ps.slice(0,12))+'</div>'}
  function processTable(ps){return '<div class="tablewrap"><table><thead><tr><th>Colaborador</th><th>Tipo</th><th>Unidade</th><th>Início</th><th>Gestor</th><th>Situação</th></tr></thead><tbody>'+ps.map(([id,p])=>'<tr class="process" data-edit="'+esc(id)+'"><td><b>'+esc(p.nome||'Sem nome')+'</b></td><td>'+esc(p.tipo||'')+'</td><td>'+esc(p.unidade||'')+'</td><td>'+esc(p.inicio||'')+'</td><td>'+esc(p.gestor||'')+'</td><td><span class="badge">'+esc(p.situacao||'ativo')+'</span></td></tr>').join('')+'</tbody></table></div>'}
  function pessoas(){return '<div class="card"><div style="display:flex;justify-content:space-between;gap:10px;align-items:center"><h2>Pessoas / Processos</h2><button class="btn primary" id="new">+ Novo processo</button></div>'+processTable(processos())+'</div>'}
  function formularios(){return '<div class="card"><h2>Links públicos permanentes</h2><p class="muted">O mesmo endereço é reutilizado; não existe um link diferente por empregado.</p>'+LINKS.map(l=>'<div class="field"><label>'+esc(l.name)+'</label><div class="linkbox"><code>'+esc(location.origin+l.path)+'</code><button class="btn" data-copy="'+esc(location.origin+l.path)+'">Copiar</button><a class="btn" target="_blank" href="'+esc(l.path)+'">Abrir</a></div></div>').join('')+'</div>'}
  function respostas(){const rs=responses();return '<div class="card"><h2>Respostas vinculadas</h2><div class="tablewrap"><table><thead><tr><th>Protocolo</th><th>Colaborador</th><th>Formulário</th><th>Ciclo</th><th>Papel</th><th>Data</th></tr></thead><tbody>'+rs.map(x=>'<tr><td><b>'+esc(x.r.protocolo||'')+'</b></td><td>'+esc(x.p.nome||'')+'</td><td>'+esc(x.r.form||'')+'</td><td>'+esc(x.r.ciclo||'')+'</td><td>'+esc(x.r.papel||'')+'</td><td>'+esc(x.r.em||'')+'</td></tr>').join('')+'</tbody></table></div></div>'}
  function pendentes(){const a=pend();return '<div class="card"><h2>Pendentes de vinculação</h2><p class="muted">Estas respostas foram recebidas, mas o sistema não teve segurança suficiente para ligá-las automaticamente a um processo.</p>'+(!a.length?'<div class="msg ok">Nenhuma resposta pendente.</div>':a.map(x=>'<div class="card" style="box-shadow:none"><b>'+esc(x.protocolo||'')+' · '+esc(x.nomeColaborador||'Sem nome')+'</b><div class="small muted">'+esc(x.formKey||'')+' · '+esc(x.unidade||'')+' · '+esc(x.dataInicio||'')+'</div><div class="small" style="margin-top:8px">Motivo: '+esc(x.motivo||'revisão necessária')+'</div><button class="btn danger" data-discard="'+esc(x.id)+'" style="margin-top:10px">Descartar da fila preservando histórico</button></div>').join(''))+'</div>'}
  function config(){return '<div class="card"><h2>Configurações</h2><p class="muted">As configurações detalhadas e textos dos formulários continuam armazenados no módulo. Nesta implantação, alterações de estrutura ficam bloqueadas até a carga histórica ser validada.</p><div class="msg ok">Backup do banco confirmado antes da implantação. Exclusões físicas não são utilizadas neste módulo.</div></div>'}
  function editor(id){const p=id?state.processos[id]:{tipo:'Onboarding',situacao:'ativo',part:'Presencial',feito:{},alin:{},bem:{},teste:{},resp:[]};return '<div class="modal"><div class="card"><h2>'+(id?'Editar processo':'Novo processo')+'</h2><div class="grid">'+[['nome','Nome'],['email','E-mail pessoal'],['emailCorporativo','E-mail corporativo'],['cargo','Cargo'],['unidade','Área / Unidade'],['inicio','1º dia na unidade'],['gestor','Gestor receptor'],['gestorEmail','E-mail do gestor'],['anjo','Anjo'],['anjoEmail','E-mail do Anjo'],['ugp','Destinatário na UGP']].map(([k,l])=>'<div class="field"><label>'+l+'</label><input '+(k==='inicio'?'type="date" ':'')+'data-pf="'+k+'" value="'+esc(p[k]||'')+'"></div>').join('')+'<div class="field"><label>Tipo</label><select data-pf="tipo"><option '+(p.tipo==='Onboarding'?'selected':'')+'>Onboarding</option><option '+(p.tipo==='Crossboarding'?'selected':'')+'>Crossboarding</option></select></div><div class="field"><label>Situação</label><select data-pf="situacao"><option '+(p.situacao==='ativo'?'selected':'')+'>ativo</option><option '+(p.situacao==='encerrado'?'selected':'')+'>encerrado</option></select></div></div><div class="actions"><button class="btn" id="cancel">Cancelar</button><div>'+(id?'<button class="btn danger" id="archive">Remover da visão</button> ':'')+'<button class="btn primary" id="save">Salvar</button></div></div></div></div>'}
  async function saveEditor(){const old=editId?state.processos[editId]:{feito:{},alin:{},bem:{},teste:{},resp:[]};const p={...old};document.querySelectorAll('[data-pf]').forEach(el=>p[el.dataset.pf]=el.value);if(!p.nome?.trim()||!p.inicio){alert('Informe nome e data de início.');return}let id=editId;if(!id){id='p'+Date.now().toString(36);state.config.ordem=state.config.ordem||[];state.config.ordem.push(id)}await api('/api/programa-integracao/processos/'+encodeURIComponent(id),{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({processo:p,ordem:state.config.ordem.indexOf(id)})});state.processos[id]=p;await api('/api/programa-integracao/config',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({config:state.config})});editId=null;await load()}
  async function discard(id){if(!confirm('Descartar esta resposta da fila? Ela continuará preservada no histórico do banco.'))return;state.config.respostasPendentes=pend().filter(x=>x.id!==id);await api('/api/programa-integracao/config',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({config:state.config})});await load()}
  function render(){if(!state)return;let body=tab==='painel'?painel():tab==='pessoas'?pessoas():tab==='formularios'?formularios():tab==='respostas'?respostas():tab==='pendentes'?pendentes():config();document.getElementById('app').innerHTML='<h1>Programa de Integração</h1><p class="muted">Onboarding · Crossboarding · acompanhamento do processo</p>'+nav()+body+(editId!==null?editor(editId==='__new'?null:editId):'');document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{tab=b.dataset.tab;render()});document.querySelectorAll('[data-edit]').forEach(r=>r.onclick=()=>{editId=r.dataset.edit;render()});document.querySelectorAll('[data-copy]').forEach(b=>b.onclick=()=>navigator.clipboard.writeText(b.dataset.copy));document.querySelectorAll('[data-discard]').forEach(b=>b.onclick=()=>discard(b.dataset.discard));document.getElementById('new')?.addEventListener('click',()=>{editId='__new';render()});document.getElementById('cancel')?.addEventListener('click',()=>{editId=null;render()});document.getElementById('save')?.addEventListener('click',saveEditor);document.getElementById('archive')?.addEventListener('click',async()=>{if(!confirm('Remover este processo da visão? O registro continuará preservado no banco.'))return;await api('/api/programa-integracao/processos/'+encodeURIComponent(editId),{method:'DELETE'});state.config.ordem=state.config.ordem.filter(x=>x!==editId);editId=null;await api('/api/programa-integracao/config',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({config:state.config})});await load()});}
  load();</script></body></html>`;
}

export function registerProgramaIntegracaoPages(app: Express) {
  app.get("/formularios/:slug", (req, res) => {
    const html = publicFormHtml(req.params.slug);
    if (!html) return res.status(404).send("Formulário não encontrado.");
    res.setHeader("Cache-Control", "no-store");
    res.type("html").send(html);
  });

  app.get("/programa-integracao", async (req: Request, res: Response) => {
    try {
      const user = await sdk.authenticateRequest(req);
      if (!user || user.role !== "admin") return res.redirect("/");
      res.setHeader("Cache-Control", "no-store");
      return res.type("html").send(adminHtml());
    } catch {
      return res.redirect("/");
    }
  });
}
