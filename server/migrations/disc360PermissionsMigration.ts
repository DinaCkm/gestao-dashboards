/**
 * Migration: Sincronizar permissões do módulo EcoDISC 360 para administradores
 *
 * Esta migration garante que todos os usuários com role 'admin' ou 'admin2'
 * tenham acesso às rotas do módulo EcoDISC 360. Ela roda automaticamente
 * durante a inicialização do servidor.
 *
 * Rotas sincronizadas:
 * - /disc360 (dashboard principal)
 * - /disc360/perfis-empresa (gestão de perfis da empresa/diretoria)\n * - /disc360/responder-convite/:token (responder pesquisa de cultura)\n * - /disc360/estrutura-organizacional\n * - /disc360/dashboard-cultura/:orgProfileId\n * - /disc360/relatorio-cultura/:orgProfileId\n * - /disc360/perfis-cargo (gestão de perfis de cargo)\n * - /disc360/responder-convite-cargo/:token (responder perfil do cargo)\n * - /disc360/relatorio-cargo/:cargoProfileId\n * - /disc360/dashboard-cargo\n * - /disc360/resultado-match (matriz de aderência pessoa x cargo)\n * - /disc360/aplicacoes (lista de aplicações DISC)\n * - /disc360/relatorio-individual/:alunoId (relatório individual)\n */\n\nimport type { getDb } from \"../db\";\n\ntype DbClient = NonNullable<Awaited<ReturnType<typeof getDb>>>;\n\nconst DISC360_ROTAS = [\n  \"/disc360\",\n  \"/disc360/perfis-empresa\",\n  \"/disc360/responder-convite/:token\",\n  \"/disc360/estrutura-organizacional\",\n  \"/disc360/dashboard-cultura/:orgProfileId\",\n  \"/disc360/relatorio-cultura/:orgProfileId\",\n  \"/disc360/perfis-cargo\",\n  \"/disc360/responder-convite-cargo/:token\",\n  \"/disc360/relatorio-cargo/:cargoProfileId\",\n  \"/disc360/dashboard-cargo\",\n  \"/disc360/resultado-match\",\n  \"/disc360/aplicacoes\",\n  \"/disc360/relatorio-individual/:alunoId\",\n];\n\n/**\n * Sincroniza as permissões de página para o módulo EcoDISC 360.\n * - Cria tabela admin_page_permissions se não existir\n * - Insere registros para admins que ainda não têm permissões\n * - Faz merge (adiciona disco360) para admins que já têm permissões\n */\nexport async function syncDisc360Permissions(database: DbClient): Promise<void> {\n  try {\n    // Passo 1: Criar tabela se não existir\n    await database.execute(\n      `CREATE TABLE IF NOT EXISTS admin_page_permissions (\n        userId INT NOT NULL PRIMARY KEY,\n        permissions JSON NOT NULL,\n        createdAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n        updatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP\n      )`\n    );\n\n    // Passo 2: Inserir permissões para admins que não têm nenhuma cadastrada\n    const disc360JsonArray = JSON.stringify(DISC360_ROTAS);\n    await database.execute(\n      `INSERT INTO admin_page_permissions (userId, permissions)\n       SELECT u.id, ? AS permissions\n       FROM users u\n       WHERE (u.role = 'admin' OR u.role = 'admin2')\n         AND u.id NOT IN (SELECT userId FROM admin_page_permissions)\n       ON DUPLICATE KEY UPDATE permissions = VALUES(permissions)`,\n      [disc360JsonArray]\n    );\n\n    // Passo 3: Para admins que já têm permissões, fazer merge\n    // (adicionar disco360 se não tiverem)\n    const adminIds = await database.execute(\n      `SELECT DISTINCT u.id FROM users u WHERE (u.role = 'admin' OR u.role = 'admin2')`\n    );\n\n    if (Array.isArray(adminIds) && adminIds.length > 0) {\n      const rows = adminIds as any[];\n      for (const row of rows) {\n        const userId = row.id;\n\n        // Buscar permissões atuais\n        const result = await database.execute(\n          `SELECT permissions FROM admin_page_permissions WHERE userId = ?`,\n          [userId]\n        );\n\n        let currentPerms: string[] = [];\n        if (Array.isArray(result) && result.length > 0) {\n          const permsData = (result[0] as any)?.permissions;\n          if (typeof permsData === \"string\") {\n            try {\n              currentPerms = JSON.parse(permsData);\n            } catch {\n              currentPerms = [];\n            }\n          } else if (Array.isArray(permsData)) {\n            currentPerms = permsData;\n          }\n        }\n\n        // Mesclar com disco360 (evitar duplicatas)\n        const mergedPerms = Array.from(new Set([...currentPerms, ...DISC360_ROTAS]));\n\n        // Atualizar\n        await database.execute(\n          `UPDATE admin_page_permissions SET permissions = ? WHERE userId = ?`,\n          [JSON.stringify(mergedPerms), userId]\n        );\n      }\n    }\n\n    console.log(\"[Disc360] Permissões sincronizadas para administradores.\");\n  } catch (error) {\n    console.error(\"[Disc360] Erro ao sincronizar permissões:\", error);\n    // Não lançar erro - a falta de sincronização não deve quebrar o servidor\n  }\n}\n
